# Usage

```
cargo run
```

- `Left click` Revive cell
- `Right click` Kill cell
- `Space` Play/Pause
- `Right` Next step
- `Up` Increase speed
- `Down` Decrease speed
- `r` Reset speed
- `q` or `Esc` Quit
